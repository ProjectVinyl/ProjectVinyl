#!/bin/bash

export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init -)"

export RAILS_ENV=production
export QUEUE=high_priority

cd $HOME/ProjectVinyl
$HOME/.rbenv/shims/bundle exec rake environment resque:work
