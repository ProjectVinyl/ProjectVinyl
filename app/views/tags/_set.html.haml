- if tags.length == 1
	- @tag = tags.first
	- @implies = @tag.implications
	- @implied = @tag.implicators
	- @aliases = @tag.aliases
	= render 'tags/bio'
- else
	.post
		%ul.tags= render partial: 'tags/thumb_h', collection: tags
%hr
