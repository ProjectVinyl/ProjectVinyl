%span.tag-set.underline
	- collection.each_with_index do |item, index|
		- if index > 0
			,
		- if index == 20
			!= "<span class=\"overflow\">"
		%a{href: item.link}
			%span<= item.name
	- if collection.length > 20
		!= "</span>"
		%a.state-toggle(data-state="show-all" data-true="(less)" data-false="(more)" href="#") (more)
