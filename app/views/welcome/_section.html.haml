%section.page.seamless.slim
	%h1
		%a{href: search_index_path(q: filter, orderby: order)}= label
	.group
		%ul.horizontal.latest.thin-scrollbar
			= render partial: 'videos/thumb/normal', collection: collection
	- if type == 'popular' && @exception_flag
		.right= fa :star_o
