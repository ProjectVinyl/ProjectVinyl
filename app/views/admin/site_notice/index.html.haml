%section.page.full-width.listing.compact
	%p All the messages that appear on the site can be defined from here.
	.items
		%span.row.heads
			%div
			%div Message
			.center(style="width:0") Active
			%div
		= render partial: 'item', collection: @notices
	%span.row.foot
		%a.button.button-bub.bub(title="add" data-title="Add Notice" data-external-form="/admin/sitenotices/new")
			= fa :plus
			Add New
