%section.page.properties.full-width
	%h1 Files
	%table
		%tr
			%td.label Media Type
			%td.field= @video.audio_only ? "Audio" : "Video"
			%td
		%tr
			%td.label File Type
			%td.field= "#{@video.mime} (#{@video.file})"
			%td
		%tr
			%td.label Dimensions
			%td.field= "#{@video.get_width}x#{@video.get_height}"
		%tr
			%td.label Size
			%td.field= "%.2f MB" % @video.file_size
		%tr
			%td.label Source
			%td.field= bool File.exist?(@video.video_path)
			%td
				%a.button{href: @video.file_link("#{@video.storage_root}/stream", "#{@video.id}#{@video.file}")} View
		- if !@video.audio_only
			%tr
				%td.label Webm
				%td.field= bool File.exist?(@video.webm_path)
				%td
					%a.button{href: @video.file_link("#{@video.storage_root}/stream", "#{@video.id}.webm")} View
		%tr
			%td.label Cover
			%td.field= bool File.exist?(@video.cover_path)
			%td
				%a.button{href: @video.file_link('public/cover', "#{@video.id}.png")} View
		%tr
			%td.label Thumbnail
			%td.field= bool File.exist?(@video.tiny_cover_path)
			%td
				%a.button{href: @video.file_link('public/cover', "#{@video.id}-small.png")} View
