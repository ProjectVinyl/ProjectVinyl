%section.page.full-width.recent-comments
	%h1.title
		Comments (
		= number_with_delimiter @comments.count
		)
		%a.right{href: user_comments_path(@user)}
			View All
			= fa :chevron_right
	%ul.comment-list= render 'comments/set', thread: @comments.records, indirect: true
