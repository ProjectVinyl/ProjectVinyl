- if !@user.description.nil? && !@user.description.blank?
	%section.page.full-width.artist-bio.removeable(data-id="#{part.id}" data-index="#{part.index}")
		%h1
			- if @modifications_allowed
				.handle(title="Arrange")= fw :th
			Bio
		.post.floater-hidden
			%span.content.bio= emotify @user.description
