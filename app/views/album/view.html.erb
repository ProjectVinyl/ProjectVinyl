<% if @album %>
		<% title @album.title %>
		<%= render partial: 'album' %>
		<%= render partial: 'items' %>
<% else %>
<%= render layout: '/layouts/error', locals: { title: 'Nothing to see here!' } do %>
This album appears to have been  moved or deleted.
<% end %>
<% end %>