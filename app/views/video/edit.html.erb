<% if @video %>
		<section class="page column-left">
			<%= form_for(@video, as: "video", url: "/ajax/update/video/cover", html: {class: "edit_video async"}) do |f| %>
			<%= f.text_field :id, hidden: true %>
			<div class="progressor">
				<div class="cover-block">
					<div class="message">
						Uploading...
					</div>
					<div class="progress">
						<div class="fill"></div>
					</div>
				</div>
			</div>
			<div id="upload">
				<div id="cover-upload" class="cover-block file-select image-selector" data-type="image">
					<span class="preview" style="background-image:url('/cover/<%= @video.id %>');"></span>
					<span class="drop-icon">
						<i class="fa fa-picture-o"></i>
						<div>Select Cover Art</div>
					</span>
					<%= f.file_field :cover %>
				</div>
			</div>
			<div class="post description resize-holder">
				<div class="actions">
					<label class="button">
						<input name="erase" type="checkbox" checked="0"></input>
						<i class="fa fa-check"></i>
						Remove current Image
					</label>
					<span class="notify bob-holder">
						<div class="bobber bob-left">
							Cover art is required to upload audio files
						</div>
						<%= f.submit "Save", class: "action right green" %>
					</span>
				</div>
				<div class="actions">
					<a class="foobar action options"><i class="fa fa-gear"></i> Options</a>
					<a class="foobar action share"><i class="fa fa-share"></i> Share</a>
					<a class="foobar action star"><i class="fa fa-star"></i></a>
					<a class="foobar action like">
						<i class="fa fa-thumbs-up"></i>
					</a>
					<a class="foobar action dislike">
						<i class="fa fa-thumbs-down"></i>
					</a>
					<a class="foobar action report right"><i class="fa fa-flag"></i> Report</a>
				</div>
				<div class="form report" data-offset="0"></div>
			</div>
			<% end %>
		</section>
		<div class="page resize-holder column-right right center">
			<section class="artist-bio">
				<span class="avatar">
					<span style="background-image:url('/avatar/<%= @artist.id %>');"></span>
				</span>
				<h1 class="resize-target">
					<a class="name" href="/artist/<%= @artist.id %>-<%= @artist.name %>"><%= @artist.name %></a>
				</h1>
				<div class="bio short"><%= raw emotify @artist.bio %></div>
			</section>
			<section>
				<h1 class="resize-target">
					<a>Discovery Queue</a>
				</h1>
				<ul class="queue">
				</ul>
			</section>
		</div>
		<section class="page column-left">
			<div class="comments"></div>
		</section>
<% else %>
<%= render layout: '/layouts/error', locals: { title: 'Access Denied' } do %>
You can't do that right now.
<% end %>
<% end %>