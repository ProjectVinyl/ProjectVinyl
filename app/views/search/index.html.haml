- title @query.length > 0 ? "Searching for #{@query}" : "Search"
.page.full-width= render partial: @derpy.nil? ? 'search/results' : 'search/warden_derpy'
.page.full-width.resize-holder
	%section
		%h1.resize-target
			%a Search
		= render partial: 'search/big_search'
